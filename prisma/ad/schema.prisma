generator client {
  provider = "prisma-client"
  output   = "../generated/ad/client"
}

datasource db {
  provider = "postgresql"
}

model User {
  id        String      @id
  bio       String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
